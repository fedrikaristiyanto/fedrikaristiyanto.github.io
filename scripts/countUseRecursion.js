/*
var number=1;
function count(number) {

    
    document.write(number + " ");

    
    var newNumber = number + 1;

    
    if (newNumber <= 10) {
        count(newNumber);
    }
}

count(number);
*/
